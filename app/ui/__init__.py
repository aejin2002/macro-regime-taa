"""UI presentation layer for the Streamlit dashboard -- theme, reusable
components, chart builders, and formatters. No strategy logic or
backtest computation lives in this package; every function here renders
values that were already computed by `macro_regime`."""
